import React from 'react'
// import { withRouter } from 'react-router-dom';

const Ambient = ({ history }) => {
    return(
        <div className='ambient'>
            <span>Окружение</span>
        </div>        
    )
}

export default Ambient
// export default withRouter(Ambient);